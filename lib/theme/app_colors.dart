import 'package:flutter/material.dart';

class AppColors {
  // ─── DARK THEME ───────────────────────────────────────────
  static const Color bg            = Color(0xFF100D10); // Noir profond
  static const Color bg2           = Color(0xFF391F1E); // Noir charbon
  static const Color card          = Color(0xFF391F1E);
  static const Color card2         = Color(0xFF2A1615);
  static const Color text          = Color(0xFFE1D5BF); // Beige clair
  static const Color subText       = Color(0xFF5D5250); // Gris fumée
  static const Color iconSecondary = Color(0xFFAB9877); // Gris sable

  // ─── LIGHT THEME ──────────────────────────────────────────
  static const Color bgLight       = Color(0xFFFDF6EE);
  static const Color bg2Light      = Color(0xFFF2E8DA);
  static const Color cardLight     = Color(0xFFFFFFFF);
  static const Color card2Light    = Color(0xFFFAF0E6);
  static const Color textLight     = Color(0xFF1A0A00);
  static const Color subTextLight  = Color(0xFF7A5C4A);
  static const Color iconSecLight  = Color(0xFF9B6A3E);

  // ─── FIRE ACCENTS (partagés dark + light) ─────────────────
  static const Color red       = Color(0xFF9B2C14);
  static const Color redAlert  = Color(0xFFFF3B30);
  static const Color orange    = Color(0xFFFF6A00);

  // ─── STATUS ───────────────────────────────────────────────
  static const Color green  = Color(0xFF4CAF50);
  static const Color yellow = Color(0xFFFFD600);
}